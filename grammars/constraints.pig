# Assignments ought to be at least 5 chars long
length(<assignment>) >= 5

# Pick only assignments who contain the letters `abc`
contains_all(<assignment>, "abc")

# Sample 20% of the total expansions
sample(<assignment>, 20)